const DB=['weapons','armors','items','skills','states','enemies','actors','troops'];
const rarity={0:['Starter','#CCCCCC'],1:['Common','#FFFFFF'],2:['Uncommon','#78FF78'],3:['Rare','#6B99E3'],4:['Epic','#FF78DD'],5:['Legendary','#FFCA2B'],6:['Mythic','#7B68EE']};
const rarityOrder=[0,1,2,3,4,5,6];
let db={},config={},usedIds=[];
const $=s=>document.querySelector(s); const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function noteTag(note,key){const m=String(note||'').match(new RegExp('<'+key.replace(/[.*+?^${}()|[\\]\\]/g,'\\$&')+'\\s*:\\s*([^>]+)>','i'));return m?m[1].trim():''}
function allTags(note,key){const r=new RegExp('<'+key.replace(/[.*+?^${}()|[\\]\\]/g,'\\$&')+'\\s*:\\s*([^>]+)>','gi'),a=[];let m;while((m=r.exec(String(note||''))))a.push(m[1].trim());return a}
function parseRarity(note){const m=String(note||'').match(/<Rarity:\s*(\d+)>/i);const n=m?Number(m[1]):null;return n!==null&&rarity[n]?n:null}
function cleanText(s){return String(s??'').replace(/\\c\[\d+\]/g,'').replace(/\\[.!|{}]/g,' ').replace(/\s+/g,' ').trim()}
function icon(idx){if(idx==null||idx<0)return '';const x=idx%16,y=Math.floor(idx/16);return `<span class="icon" style="background-position:-${x*32}px -${y*32}px"></span>`}
function rarityTag(r){if(r==null)return '<span class="tag">Unassigned</span>';const x=rarity[r];return `<span class="tag" style="color:${x[1]};border-color:${x[1]}55;background:${x[1]}14">${esc(x[0])} · Tier ${r}</span>`}
function enemyCategory(e){return noteTag(e.note,'EnemyCategory').trim().toLowerCase()||'uncategorized'}
function enemyLabel(c){const labels={'normal':'Normal','champion':'Champion','elite':'Elite','elite champion':'Elite Champion','mini boss':'Mini Boss','boss':'Boss','abyss normal':'Abyss · Normal','abyss champion':'Abyss · Champion','abyss elite':'Abyss · Elite','abyss elite champion':'Abyss · Elite Champion','abyss mini boss':'Abyss · Mini Boss','abyss boss':'Abyss · Boss','uncategorized':'Uncategorized'};return labels[c]||c.replace(/\b\w/g,m=>m.toUpperCase())}
function enemyTag(c){return `<span class="tag">${esc(enemyLabel(c))}</span>`}
function levelRange(e){const a=noteTag(e.note,'Minimum Level'),b=noteTag(e.note,'Maximum Level');return a&&b?`${esc(a)}-${esc(b)}`:a?esc(a):'—'}
function dataById(type,id){return (db[type]||[]).find(x=>x&&x.id===id)}
function validEntries(type){return (db[type]||[]).filter(x=>x&&x.name&&!/^=+/.test(x.name))}
function usedEnemies(){return usedIds.map(id=>dataById('enemies',id)).filter(e=>e&&e.name)}
function nav(active){document.querySelectorAll('.side-links a,.topnav a').forEach(a=>a.classList.toggle('active',a.dataset.page===active))}
function socialLinks(){const map={'Discord':'Discord02.png','YouTube':'YouTube02.png','X / Twitter':'Twitter02.png','itch.io':'GitHub02.png','GitHub':'GitHub02.png'};return Object.entries(config.socials||{}).filter(([,u])=>u).map(([n,u])=>`<a class="social-btn" href="${esc(u)}" target="_blank" rel="noopener noreferrer" title="${esc(n)}"><img src="assets/${map[n]||'GitHub02.png'}" alt="${esc(n)}"></a>`).join('')}
function sidebar(){return `<aside class="sidebar"><p class="eyebrow">Navigate</p><div class="side-links"><a data-page="home" href="#home">Overview</a><a data-page="enemies" href="#enemies">Enemies</a><a data-page="weapons" href="#weapons">Weapons</a><a data-page="armors" href="#armors">Armor</a><a data-page="items" href="#items">Items</a><a data-page="skills" href="#skills">Skills</a><a data-page="states" href="#states">Status Effects</a><a data-page="actors" href="#actors">Characters</a></div><p class="eyebrow">Rarity Scale</p><div class="rarity-list">${rarityOrder.map(r=>`<div class="rarity-row"><i class="rarity-dot" style="background:${rarity[r][1]};color:${rarity[r][1]}"></i>${rarity[r][0]}<span class="rarity-tier">T${r}</span></div>`).join('')}</div><p class="sidebar-note">Rarity applies to equipment only. Enemy categories use their own classification.</p></aside>`}
function shell(active,content){return `<header class="topbar"><a class="brand" href="#home"><span class="brand-mark">IE</span><span><div class="brand-title">INFINITE ECHOES</div><div class="brand-sub">IN THE INFINITE ABYSS</div></span></a><nav class="topnav"><a data-page="home" href="#home">Overview</a><a data-page="enemies" href="#enemies">Enemies</a><a data-page="weapons" href="#weapons">Weapons</a><a data-page="armors" href="#armors">Armor</a><a data-page="items" href="#items">Items</a><a data-page="skills" href="#skills">Skills</a><a data-page="states" href="#states">Status</a><a data-page="actors" href="#actors">Characters</a></nav><div class="top-actions">VERSION ${esc(config.version||'0.0.0')}</div></header><div class="layout">${sidebar()}<main class="main"><div class="content">${content}</div></main></div><footer class="footer"><span>${esc(config.gameTitle)} · ${esc(config.dataStatus||'Development database')}</span><span>Last updated ${esc(config.lastUpdated||'—')} <span class="socials">${socialLinks()}</span></span></footer>`}
function home(){const counts={weapons:validEntries('weapons').length,armors:validEntries('armors').length,items:validEntries('items').length,skills:playableSkills().length,states:validEntries('states').length,actors:validEntries('actors').filter(a=>['Roy','Dog','Willy'].includes(a.name)).length,enemies:usedEnemies().length};const cards=[['Weapons','weapons',`${counts.weapons} equipment entries · Tiered weapons and gear.`],['Armor & Accessories','armors',`${counts.armors} entries · Tiered defensive and accessory equipment.`],['Items','items',`${counts.items} entries · Materials, consumables, and key items.`],['Skills','skills',`${counts.skills} player skills · Enemy-only skills stay with enemies.`],['Status Effects','states',`${counts.states} conditions from the current database.`],['Characters','actors',`${counts.actors} playable party members in the current build.`],['Enemies','enemies',`${counts.enemies} named enemies currently referenced by Troops.json.`]];return shell('home',`<section class="home-hero fade"><div class="page-kicker">${esc(config.subtitle)}</div><h1>${esc(config.gameTitle)}</h1><p>A living reference for the things you can discover in the Abyss. Game-facing information is separated from RPG Maker setup data, so internal notetags are not treated as player-facing encyclopedia text.</p><div class="hero-meta"><span class="pill">VERSION ${esc(config.version)}</span><span class="pill">UPDATED ${esc(config.lastUpdated)}</span><span class="pill">SPOILERS OFF</span></div></section><div class="category-grid fade">${cards.map(c=>`<a class="category" href="#${c[1]}"><h3>${c[0]}</h3><p>${c[2]}</p></a>`).join('')}</div><section class="recent"><div class="section-title">Recently Cataloged</div><div class="grid">${usedEnemies().slice(-6).reverse().map(e=>card('enemies',e)).join('')||'<div class="empty">No enemy entries yet.</div>'}</div></section><section class="recent"><div class="section-title">Version History</div><div class="history">${(config.versionHistory||[]).map(v=>`<div class="history-row"><span><b>${esc(v.version)}</b> · ${esc(v.changes)}</span><span class="version">${esc(v.date)}</span></div>`).join('')}</div></section>`)}
function sortControls(type){let options=type==='enemies'?['name-az','name-za','hp-high','hp-low']:type==='weapons'||type==='armors'?['tier-low','tier-high','name-az','name-za','price-low','price-high']:['name-az','name-za','id-low','id-high'];const labels={'name-az':'Name A–Z','name-za':'Name Z–A','tier-low':'Tier Low → High','tier-high':'Tier High → Low','price-low':'Price Low → High','price-high':'Price High → Low','hp-high':'HP High → Low','hp-low':'HP Low → High','id-low':'ID Low → High','id-high':'ID High → Low'};return `<div class="sort-wrap">SORT <select class="sort-select" id="sort">${options.map(o=>`<option value="${o}">${labels[o]}</option>`).join('')}</select></div>`}
function listPage(type,title,desc,filters){return shell(type,`<div class="page-head fade"><div><div class="page-kicker">Field Codex · Database</div><h1 class="page-title">${title}</h1><p class="page-desc">${desc}</p></div><input class="search" id="search" placeholder="Search ${title.toLowerCase()}..." /></div>${filters?`<div class="filters" id="filters">${filters.map((f,i)=>`<button class="filter ${i===0?'active':''}" data-filter="${esc(f[0])}">${esc(f[1])}</button>`).join('')}</div>`:''}<div class="list-tools"><span class="count" id="count"></span>${sortControls(type)}</div><div class="grid fade" id="cards"></div>`)}
function card(type,x){if(type==='enemies'){const c=enemyCategory(x),p=x.params||[];return `<article class="card" data-id="${x.id}"><div class="topline">${enemyTag(c)}<span class="zone">${esc(noteTag(x.note,'Tattle Category')||'UNASSIGNED ZONE')}</span></div><div class="enemy-card-copy"><h3>${esc(x.name)}</h3><p>${esc(cleanText(noteTag(x.note,'Info')).slice(0,125)||'Study entry available.')}</p><div class="statline"><span>HP <b>${p[0]??0}</b></span><span>ATK <b>${p[2]??0}</b></span><span>DEF <b>${p[3]??0}</b></span><span class="lv">LV ${levelRange(x)}</span></div></div><img class="enemy-art" src="assets/enemies_src/${encodeURIComponent(x.battlerName||'')}.png" onerror="this.style.display='none'" alt=""></article>`}const r=parseRarity(x.note);let art=type==='weapons'?weaponArt(x):'';return `<article class="card" data-id="${x.id}"><div class="topline">${type==='weapons'||type==='armors'?rarityTag(r):`<span class="tag">${type==='skills'?'Player Skill':type==='states'?'Status':'Database'}</span>`}<span class="zone">#${x.id}</span></div><div class="item-title">${icon(x.iconIndex)}<h3>${esc(x.name)}</h3></div>${art?`<img class="weapon-art" src="${art}" alt="">`:''}<p>${esc(cleanText(x.description)||'No player-facing description recorded.')}</p><div class="statline"><span>${type==='weapons'||type==='armors'?'PRICE':'ID'} <b>${type==='weapons'||type==='armors'?(x.price??0):x.id}</b></span>${r!=null?`<span style="color:${rarity[r][1]}">T${r}</span>`:''}</div></article>`}
const weaponMap={'Twig':'Stick 1.png','Stick':'Stick 1.png','Old Frying Pan':'Frying Pan.png','Old Wooden Club':'Club.png','Worn-Out Wooden Bat':'Worn-out Bat.png',"Beginner's Bow":'Normal Bow.png','Squeaky Hammer':'Worn out Hammer.png','Magical Cane':'Wand of Energizing Nectar.png','Toy Golf Club':'Golf Club.png','Wooden Bat':'Wooden Bat.png','Short Leaf Sword':'Short Leaf Sword.png','Leafwhipper':'Leafwhipper.png','Wooden Bow':'Normal Bow.png','Worn-Out Hammer':'Worn out Hammer.png','Jello Bello B':'Jello Bello B.png','Wooden Sword':'Wooden Sword.png','Palce Holder (Bow)':'Normal Bow.png','Sharp Dagger':'Dagger.png','Jello Bello R':'Jello Bello R.png','Stony Bow':'Stony Bow.png','The Electronic Hammer':'The Electronic Hammer.png','Wand of Energizing Nectar':'Wand of Energizing Nectar.png','The Hammer of Destruction':'The Hammer of Destruction.png','Sword':'Wooden Sword.png','Axe':'Club.png','Pistol Gun':'Golden Bow.png','Bow':'Normal Bow.png','Worn-Out Dagger':'Dagger.png','Magical Cane TEST':'Wand of Energizing Nectar.png','Sharp Dagger TEST':'Dagger.png','Wand of Energizing Nectar TEST':'Wand of Energizing Nectar.png','Normal Dog Fangs':'Dagger.png',"Willy's Light Fist":'Club.png'};
function weaponArt(x){const f=weaponMap[x.name];return f?`assets/weapons_src/${encodeURIComponent(f)}`:''}
function renderList(type){const q=($('#search')?.value||'').toLowerCase();let arr=type==='enemies'?usedEnemies():validEntries(type);if(type==='skills')arr=playableSkills();const active=$('.filter.active')?.dataset.filter;if(type==='enemies'&&active&&active!=='all')arr=arr.filter(e=>enemyCategory(e)===active);if(type==='weapons'||type==='armors'){const activeTier=$('.filter.active')?.dataset.filter;if(activeTier&&activeTier!=='all')arr=arr.filter(x=>parseRarity(x.note)===Number(activeTier))}arr=arr.filter(x=>`${x.name} ${cleanText(x.description)}`.toLowerCase().includes(q));const s=$('#sort')?.value||'name-az';arr.sort((a,b)=>sorter(type,s,a,b));$('#count').textContent=`${arr.length} entr${arr.length===1?'y':'ies'}`;$('#cards').innerHTML=arr.map(x=>card(type,x)).join('')||'<div class="empty">No matching entries.</div>';document.querySelectorAll('.card').forEach(c=>c.onclick=()=>location.hash=`#${type}/${c.dataset.id}`)}
function sorter(type,s,a,b){if(s.startsWith('tier'))return ((parseRarity(a.note)??99)-(parseRarity(b.note)??99))*(s==='tier-high'?-1:1);if(s.startsWith('price'))return ((a.price??0)-(b.price??0))*(s==='price-high'?-1:1);if(s==='hp-high'||s==='hp-low')return ((b.params?.[0]??0)-(a.params?.[0]??0))*(s==='hp-high'?1:-1);if(s==='id-low'||s==='id-high')return (a.id-b.id)*(s==='id-low'?1:-1);return a.name.localeCompare(b.name)*(s==='name-az'?1:-1)}
function playableSkills(){const ids=new Set();[1,2,3].forEach(cid=>{const c=(db.classes||[]).find(x=>x&&x.id===cid);if(!c)return;allTags(c.note,'Learn Skill').forEach(v=>ids.add(Number(v)));(c.learnings||[]).forEach(l=>ids.add(l.skillId))});return (db.skills||[]).filter(s=>s&&s.name&&ids.has(s.id)).filter(s=>!enemySkillIds().has(s.id)).filter(s=>!/^Attack$|^Guard$|^Do Nothing$|^Escape$|^Wait$|^Counter$/.test(s.name))}
function enemySkillIds(){const set=new Set();usedEnemies().forEach(e=>(e.actions||[]).forEach(a=>set.add(a.skillId)));return set}
function detailPage(type,id){const x=dataById(type,Number(id));if(!x||(!x.name))return shell(type,`<div class="empty">Entry not found.</div>`);if(type==='enemies')return shell(type,enemyDetail(x));return shell(type,genericDetail(type,x))}
function detailArt(type,x){if(type==='weapons'){const f=weaponArt(x);return f?`<img src="${f}" alt="">`:''}return icon(x.iconIndex)}
function enemySkillCards(e){const ids=[...(e.actions||[])].map(a=>a.skillId).filter(Boolean);const unique=[...new Set(ids)];return unique.map(id=>dataById('skills',id)).filter(s=>s&&s.name).map(s=>`<div class="skill-card"><h4>${esc(s.name)}</h4><p>${esc(cleanText(s.description)||'No player-facing skill description recorded.')}</p></div>`).join('')}
function enemyElements(e){const rates=(e.traits||[]).filter(t=>t.code===11);const atk=(e.traits||[]).filter(t=>t.code===31);return `<div class="element-box"><div class="element-group"><strong>WEAKNESSES</strong><p>${rates.filter(t=>t.value>1).length?'Element data present':'N/A'}</p></div><div class="element-group"><strong>RESISTANCES</strong><p>${rates.filter(t=>t.value<1).length?'Element data present':'N/A'}</p></div><div class="element-group"><strong>ATTACK ELEMENT</strong><p>${atk.length?'Element data present':'N/A'}</p></div><div class="element-group"><strong>TRAITS</strong><p>${(e.traits||[]).length?`${e.traits.length} gameplay traits recorded`:'N/A'}</p></div></div>`}
function statRows(e){const p=e.params||[];const colors=['#e34e78','#b97ae9','#f0c64f','#63b0e9','#bd79ed','#62d58e','#62d58e','#ed4ac7'];const max=Math.max(...p,1);return [['HP',0],['MP',1],['ATK',2],['DEF',3],['M.ATK',4],['M.DEF',5],['AGI',6],['LUK',7]].map(([n,i])=>`<div class="stat-row"><span>${n}</span><div class="bar-track"><div class="bar-fill" data-width="${Math.max(2,Math.round((p[i]||0)/max*100))}%" style="background:${colors[i]}"></div></div><span class="stat-value">${p[i]??0}</span></div>`).join('')}
function enemyDetail(e){const info=cleanText(noteTag(e.note,'Info'));const skills=enemySkillCards(e);return `<div class="detail-page fade"><a class="back" href="#enemies">← Back to Enemy Bestiary</a><div class="detail-top"><div class="detail-art"><img src="assets/enemies_src/${encodeURIComponent(e.battlerName||'')}.png" onerror="this.style.display='none'" alt=""></div><div><div>${enemyTag(enemyCategory(e))} <span class="tag">${esc(noteTag(e.note,'Tattle Category')||'Unassigned Zone')}</span></div><h1>${esc(e.name)}</h1><p class="detail-description">${esc(info||'No Study entry recorded yet.')}</p></div></div><div class="detail-layout"><section class="panel"><div class="panel-title">Base Stats</div>${statRows(e)}<div class="fact-grid"><div class="fact"><strong>${e.exp??0}</strong><small>EXP</small></div><div class="fact"><strong>${e.gold??0}</strong><small>GOLD</small></div><div class="fact"><strong>${levelRange(e)}</strong><small>LEVEL</small></div></div></section><section class="panel"><div class="panel-title">Weaknesses & Traits</div>${enemyElements(e)}<div class="notice">Element names are left as N/A until the game's element-name source is included. Internal RPG Maker notetags are not displayed here.</div></section><section class="panel"><div class="panel-title">Study</div><p class="detail-description">This is the enemy's <b>Study</b> entry, based on the Tattle Log information in the game. It is not treated as a generic database description.</p></section><section class="panel"><div class="panel-title">Enemy Skills</div>${skills?`<div class="skill-list">${skills}</div>`:'<p class="detail-description">No named enemy skills are currently recorded for this enemy.</p>'}</section></div></div>`}
function genericDetail(type,x){const r=(type==='weapons'||type==='armors')?parseRarity(x.note):null;const desc=cleanText(x.description);let body='';if(type==='skills')body=`<section class="panel"><div class="panel-title">Skill Details</div><div class="data-list"><div class="data-row"><span class="data-label">Type</span><span class="data-value">Player Skill</span></div><div class="data-row"><span class="data-label">Icon</span><span class="data-value">${x.iconIndex}</span></div></div></section>`;else body=`<section class="panel"><div class="panel-title">Entry Details</div><div class="data-list"><div class="data-row"><span class="data-label">Database ID</span><span class="data-value">#${x.id}</span></div>${(type==='weapons'||type==='armors')?`<div class="data-row"><span class="data-label">Price</span><span class="data-value">${x.price??0}</span></div><div class="data-row"><span class="data-label">Tier</span><span class="data-value">${r!=null?`${rarity[r][0]} · Tier ${r}`:'Unassigned'}</span></div>`:''}</div></section>`;return `<div class="detail-page fade"><a class="back" href="#${type}">← Back to ${typeTitle(type)}</a><div class="detail-top"><div class="detail-art">${detailArt(type,x)}</div><div><div>${(type==='weapons'||type==='armors')?rarityTag(r):`<span class="tag">${type==='skills'?'Player Skill':typeTitle(type)}</span>`}</div><h1>${esc(x.name)}</h1><p class="detail-description">${esc(desc||'No player-facing description recorded.')}</p></div></div><div class="detail-layout">${body}<section class="panel"><div class="panel-title">Player-Facing Information</div><p class="detail-description">Only information intended for the encyclopedia is shown here. RPG Maker implementation notes, formulas, control tags, and other internal notetags are kept out of the public page.</p></section></div></div>`}
function typeTitle(t){return ({weapons:'Weapons',armors:'Armor & Accessories',items:'Items',skills:'Skills',states:'Status Effects',actors:'Characters',enemies:'Enemy Bestiary'})[t]||t}
function page(type){if(type==='home')return home();if(type.includes('/')){const [t,id]=type.split('/');return detailPage(t,id)}const meta={enemies:['Enemy Bestiary',`${usedEnemies().length} named enemies currently referenced by Troops.json. Enemy setup tags are not displayed as player-facing information.`],weapons:['Weapons','Tiered weapons using the current RPG Maker database and provided artwork.'],armors:['Armor & Accessories',`${validEntries('armors').length} armor/accessory entries. Sorting and tier filters use the actual <Rarity:> values.`],items:['Items',`${validEntries('items').length} item entries from the current database. Item implementation tags are kept out of public details.`],skills:['Skills',`${playableSkills().length} player-facing skills. Enemy-only actions are shown on the relevant enemy pages instead.`],states:['Status Effects',`${validEntries('states').length} status effects from the current database.`],actors:['Characters','Playable party members from the current actor/class setup.']};const m=meta[type];let filters=null;if(type==='enemies')filters=[['all','All'],['normal','Normal'],['champion','Champion'],['elite','Elite'],['elite champion','Elite Champion'],['mini boss','Mini Boss'],['boss','Boss'],['abyss normal','Abyss · Normal'],['abyss champion','Abyss · Champion'],['abyss elite','Abyss · Elite'],['abyss boss','Abyss · Boss'],['uncategorized','Uncategorized']];if(type==='weapons'||type==='armors')filters=[['all','All'],...rarityOrder.map(r=>[String(r),`T${r} · ${rarity[r][0]}`])];return listPage(type,m[0],m[1],filters)}
async function load(){for(const f of DB)db[f]=await fetch(`data/${f}.json`).then(r=>r.json());config=await fetch('data/site-config.json').then(r=>r.json());usedIds=(await fetch('data/enemy-used-ids.json').then(r=>r.json())).usedEnemyIds||[];route()}
function route(){const raw=(location.hash||'#home').slice(1)||'home';document.body.innerHTML=page(raw);const base=raw.split('/')[0];nav(base);if(!raw.includes('/')&&base!=='home'){renderList(base);$('#search')?.addEventListener('input',()=>renderList(base));$('#sort')?.addEventListener('change',()=>renderList(base));document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderList(base)})}if(raw.includes('/'))setTimeout(animateBars,70)}
function animateBars(){document.querySelectorAll('.bar-fill').forEach((b,i)=>requestAnimationFrame(()=>setTimeout(()=>b.style.width=b.dataset.width,i*45)))}
window.addEventListener('hashchange',route);load();
